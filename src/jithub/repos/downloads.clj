
(ns jithub.repos.downloads)

;; Public

(defn all
    "List downloads for a repo"
    )

(defn show
    "Fetch a specific download for a repo"
    )

(defn create
    "Create a new download"
    )

(defn delete
    "Delete a specific download"
    )

